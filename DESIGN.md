---
name: 博益物流控制台
description: 基于 Cal.com 产品界面语言的物流运营后台设计系统
colors:
  canvas: "#FFFFFF"
  surface: "#F8F9FA"
  surface-muted: "#F5F5F5"
  ink: "#111111"
  active: "#242424"
  text: "#374151"
  muted: "#6B7280"
  line: "#E5E7EB"
  focus: "#3B82F6"
  success: "#10B981"
  warning: "#F59E0B"
  danger: "#EF4444"
typography:
  cjk: "Source Han Sans SC"
  latin: "Inter"
  ui: "Inter, Source Han Sans SC, system-ui, -apple-system, sans-serif"
  mono: "JetBrains Mono, Consolas, monospace"
rounded:
  xs: "4px"
  sm: "6px"
  md: "8px"
  lg: "12px"
spacing:
  xs: "4px"
  sm: "8px"
  md: "12px"
  lg: "16px"
  xl: "24px"
  2xl: "32px"
  3xl: "48px"
---

# 博益物流控制台设计规范

## 1. 适用范围

本规范是 Console 原生页面的唯一视觉与交互基线，适用于登录、概览、运单、物流跟踪、回单、客户服务、财务、调度、专线、自动化、账号与系统设置页面。新增页面必须先复用本文件定义的令牌和组件，不得自行建立另一套颜色、字体、圆角或交互规则。

设计参考为 [Cal.com design-md](https://getdesign.md/cal/design-md)，采用其干净、中性、偏开发者工具的产品界面语言，并针对中文物流后台、高密度表格、连续操作和移动端触控重新约束。这里只复用设计原则，不复制 Cal.com 品牌标识或营销页面结构。

以下内容不改造内部视觉：

- `console/templates/waybill_print.html` 及热敏打印底版，打印尺寸和坐标保持独立规范。
- 韵达、融辉等第三方原页 iframe 的内部 DOM 与样式。Console 只统一它们的外层标题、边界、工具栏、加载、错误和聚焦工作区。

## 2. 设计方向

**Creative North Star：Calm Operations Console**

界面以白色画布、近黑主操作、浅灰分组和一像素边界建立秩序。层级主要来自字体、间距和分隔线，阴影只用于弹层。项目红色 Logo 保留品牌识别，但红色不再作为导航或普通操作强调色。

工作场景是调度员和管理员在办公室显示器、触控笔记本或手机上连续处理运单、回单、客服和自动化任务。界面需要在长时间使用中保持清晰、稳定、低噪声。

核心原则：

1. 任务优先。用户进入页面后先看到标题、真实状态、筛选条件和主操作。
2. 近单色。黑白灰承担结构，蓝色只表达焦点、链接和信息状态，业务状态色只表达状态。
3. 平面优先。普通容器依靠边界与间距，不使用装饰性阴影、渐变、玻璃拟态或彩色侧边条。
4. 密集但可扫描。表格和表单允许高密度，标签、行高、列对齐和操作层级必须稳定。
5. 移动端重排。窄屏保留核心能力，使用底栏、单列、抽屉和局部横向滚动，不缩小成桌面截图。
6. 真实状态。加载、空、错误、成功、无权限和第三方不可达均显式表达，不使用模拟数据或模糊兜底。

## 3. 颜色

### 3.1 中性色

| 令牌 | 色值 | 用途 |
| --- | --- | --- |
| `--canvas` | `#FFFFFF` | 页面、侧栏、表格与主要工作区 |
| `--surface` | `#F8F9FA` | 工具栏、筛选区、弱分组与悬停 |
| `--surface-muted` | `#F5F5F5` | 选中项、禁用区和次级背景 |
| `--ink` | `#111111` | 标题、主按钮和最高强调文本 |
| `--active` | `#242424` | 主按钮悬停和按下状态 |
| `--text` | `#374151` | 正文、表格与常规标签 |
| `--muted` | `#6B7280` | 辅助说明、元数据和占位符 |
| `--line` | `#E5E7EB` | 一像素边界、表格行与分隔线 |

`#6B7280` 是白色背景上允许使用的最浅常规小文本色。更浅颜色只能用于非文字装饰或禁用状态，并且禁用状态仍需有文本说明。

### 3.2 语义色

| 令牌 | 色值 | 用途 |
| --- | --- | --- |
| `--focus` | `#3B82F6` | 键盘焦点、输入焦点、文本链接、信息状态 |
| `--success` | `#10B981` | 成功、已完成、已连接 |
| `--warning` | `#F59E0B` | 待处理、需要注意、部分完成 |
| `--danger` | `#EF4444` | 错误、异常、删除和不可逆操作 |

颜色不能成为状态的唯一载体。徽标、提示和表格状态必须同时提供可读文本，必要时配合统一 Feather 图标。

### 3.3 使用规则

- 主按钮固定使用 `#111111` 背景与白色文字。
- 当前导航使用 `#F5F5F5` 背景与 `#111111` 文字，不使用红色侧边条。
- 焦点环使用两像素蓝色描边和两像素偏移，不与错误红色混用。
- 危险按钮只有在操作确实具有破坏性时使用红色文字或浅红背景。
- 禁止渐变文字、大面积红色背景、彩色阴影和无业务含义的彩色图标底板。

## 4. 字体

### 4.1 字体资产

- 中文：`Source Han Sans SC`。首屏界面字使用 `SourceHanSansCN-UI.woff2`，动态业务数据优先使用 `SourceHanSansCN-Common.woff2`，稀有字符再回退到完整的 `SourceHanSansCN-VF.ttf.woff2`；三层均来自同一思源黑体可变字体，字形与度量保持一致。
- 英文与数字：`Inter`，首屏使用实际裁剪后的本地拉丁字符资源 `console/static/assets/fonts/InterVariable-Latin.woff2`。
- 单号和代码：继承 UI 字体并启用 `tabular-nums`。只有真实代码块才使用 `JetBrains Mono, Consolas, monospace`。
- 字体必须通过 `@font-face`、`font-display: swap` 和本地静态资源加载，不得依赖 Google Fonts 或其他在线字体服务。

Inter 使用拉丁字符 `unicode-range`，因此中英文混排时英文与数字使用 Inter，中文自动回退到思源黑体。页面必须预加载 `InterVariable-Latin.woff2` 与 `SourceHanSansCN-UI.woff2`，不得直接预加载 7 MB 以上的完整中文字体。

### 4.2 静态资源性能规范

- 品牌 Logo 使用内容哈希命名的 `console/static/assets/boyi-logistics-logo-7e1f2994.webp`，按页面最大显示宽度的两倍导出；禁止重新引用 3000px 级原图作为首屏资源。
- Feather Icons 固定使用本地锁定版本 `console/static/vendor/feather-4.29.2.min.js`，并放在页面底部、业务脚本之前加载；禁止在 `<head>` 中同步请求第三方 CDN。
- 未上传头像时使用本地 `console/static/assets/avatar-placeholder.svg`，不得为默认头像发起第三方网络请求。
- 带版本参数或内容/版本哈希文件名的资源使用一年不可变缓存；其他静态资源使用短缓存与后台重新验证。更新字体二进制时必须同步更新 CSS 与 preload URL 的版本参数。
- HTML、CSS、JavaScript、JSON、SVG 和 XML 在客户端支持时使用 gzip，并返回 `Vary: Accept-Encoding`；字体、PNG、WebP 等已压缩资源不得重复压缩。

### 4.3 字号与行高

Cal.com 原始显示级字号保留为设计上限，后台产品页面不使用营销式超大标题。

| 角色 | 字号 / 行高 | 字重 | 用途 |
| --- | --- | --- | --- |
| Display XL | `64px / 72px` | 600 | 仅品牌或公开说明页，不用于 Console |
| Display L | `48px / 56px` | 600 | 仅品牌或公开说明页，不用于 Console |
| Display M | `36px / 44px` | 600 | 仅少量独立空状态或引导，不用于表格页 |
| Page title | `28px / 36px` | 600 | 桌面一级页面标题 |
| Section title | `22px / 30px` | 600 | 主要工作区标题 |
| Component title | `18px / 26px` | 600 | 面板、弹层和分区标题 |
| Strong body | `16px / 24px` | 500 或 600 | 重要说明与移动端正文 |
| Body | `14px / 20px` | 400 | 表单、表格和常规产品文字 |
| Caption | `13px / 18px` | 400 或 500 | 辅助信息、时间和元数据 |
| Button / Nav | `14px / 20px` | 500 或 600 | 操作与导航 |

产品 UI 使用固定 `rem` 字号，不使用流动字号。页面标题最大 `1.75rem`，移动端降为 `1.375rem`。标题字距不小于 `-0.02em`，正文不使用全大写英文句子。

## 5. 间距、圆角与边界

间距采用四像素基线：`4, 8, 12, 16, 24, 32, 48, 96px`。相关控件间使用 8 到 12px，不同工作区之间使用 24 到 32px。禁止随意新增 5px、17px、27px 等无令牌值。

圆角：

- `4px`：小标签、日期单元格和紧凑图标底板。
- `6px`：输入、按钮、分页、标签页和导航选中项。
- `8px`：表格容器、工具栏和普通面板。
- `12px`：对话框、抽屉和大型独立工作区。
- `999px`：仅状态徽标与真正的胶囊分段控制。

普通表面使用 `1px solid #E5E7EB`，不叠加宽模糊阴影。弹层可使用 `0 8px 24px rgba(17, 17, 17, 0.12)`。卡片和输入禁止使用 24px 以上圆角。

## 6. 应用壳层

### 6.1 桌面

- 左侧栏宽度 `232px`，白色背景，右侧一像素边界。
- Logo 位于顶部，导航占据中部，用户资料与退出操作位于底部。
- 当前导航为浅灰底、近黑文字，不使用彩色侧边条。
- 主内容顶部显示页面标题、说明、已打开模块标签、全局运单搜索和页面操作。
- 主内容最大阅读宽度由页面类型决定，数据表和地图可铺满可用宽度，普通表单保持可读列宽。

### 6.2 移动端

- 顶部品牌栏固定显示当前页面上下文。
- 底部保留首页、三个用户可配置入口和“更多”，并适配 `safe-area-inset-bottom`。
- 触控目标不小于 `44 × 44px`。
- “更多”使用原生 `dialog` 底部面板，支持 Esc、焦点锁定、轻触遮罩关闭和触发点焦点恢复。

## 7. 组件基线

### 7.1 按钮

- 桌面高度 `40px`，触屏和移动端最小高度 `44px`。
- 主按钮：近黑背景、白色文字、`6px` 圆角，无装饰性阴影。
- 次按钮：白色背景、一像素边界、近黑文字。
- 三级按钮：透明背景，用于行内操作和图标动作。
- 危险按钮：白色或浅红背景、红色文字，并提供明确动词与对象。
- 标签采用“动词 + 对象”，例如“保存设置”“新增运单”“退出登录”，不使用含糊的“确定”。

状态必须覆盖默认、悬停、焦点、按下、禁用、加载、错误和成功。加载时保留按钮宽度并使用文本或小型进度反馈，不允许重复提交。

### 7.2 输入与筛选

- 输入高度 `40px`，移动端 `44px`，`6px` 圆角，白底与一像素边界。
- 所有输入保留可见 `<label>`。占位符只提供示例或提示，不替代标签。
- 焦点使用蓝色外环。错误使用红色边界、错误文本和 `aria-describedby`。
- 搜索与筛选优先排成单层工具栏，复杂条件进入可展开筛选区或移动端抽屉。
- 分段控制使用浅灰容器，当前项为白底、近黑文字和轻微结构阴影。

### 7.3 表格与列表

- 表头为 `13px`、500 或 600 字重，使用浅灰背景或白底下边界。
- 数据行为 `14px / 20px`，常规桌面行高 44 到 48px。
- 数字、金额、时间和单号启用 `tabular-nums`。
- 行操作靠右，主操作直接显示，低频操作进入菜单。不能依赖悬停才能发现核心操作。
- 宽表只在自身容器中横向滚动，页面 `body` 不产生横向滚动。
- 移动端根据业务重排为摘要列表或保留局部横向滚动，不隐藏关键状态和主操作。

### 7.4 状态徽标与提示

- 徽标使用轻背景、深色文字、`4px` 或胶囊圆角，最小高度 24px。
- 一般提示使用完整一像素边框和浅色背景，不使用粗彩色侧边条。
- 成功、警告和错误信息必须说明发生了什么以及下一步能做什么。
- 空状态包含清楚标题、原因或筛选提示，以及在适用时提供主操作。不得插入虚构业务记录。

### 7.5 面板、对话框与抽屉

- 页面分组优先使用间距、标题和分隔线，只有独立可操作区域才使用面板。
- 禁止面板内再嵌套同视觉权重的面板。
- 对话框使用原生 `<dialog>`，标题、关闭按钮和主操作顺序固定。
- 破坏性或批量操作需要确认，普通编辑优先使用行内展开或抽屉。
- 第三方 iframe 外层必须提供加载、认证失效、接口错误与重试状态。

## 8. 页面类型

### 概览与监控

真实计数使用紧凑指标条，不使用夸张的大数字卡片。待办类别、来源状态和详情工作区通过列表与分隔线组织，点击项需保留明确选中状态。

### 运单、回单、客服与财务

标题和主操作位于顶部，筛选区紧随其后，结果表格是页面主角。详情使用对话框或抽屉，状态、来源和失败原因保持可追溯。

### 录单、调度与第三方原页

表单、地图或 iframe 作为连续工作区，不再套入装饰性卡片网格。桌面允许并排，移动端按任务顺序单列或进入全屏专注模式。

### 自动化与账号管理

运行状态、账号状态、凭据是否完整和可执行动作必须直接可读。不能在页面回显密码、Cookie、Token、验证码或第三方完整登录态。

### 登录

桌面使用清楚的产品说明区和登录表单区，移动端只保留品牌、标题、说明和表单。表单错误使用 `role="alert"`，登录按钮提供提交中状态。

## 9. 交互与动效

- 常规状态切换为 120 到 200ms，使用 `cubic-bezier(0.22, 1, 0.36, 1)`。
- 只动画 `opacity`、`transform`、颜色、边界和短半径阴影，不使用 `transition: all`，不动画布局属性。
- 产品页面不使用编排式入场动画。
- `prefers-reduced-motion: reduce` 下取消非必要动效。
- 键盘焦点必须始终可见，标签页支持方向键时应使用 roving tabindex。

## 10. 响应式与无障碍

- 内容驱动断点：`640px`、`768px`、`1024px`。
- `320px` 宽度不得出现页面级横向滚动或操作不可达。
- 支持 200% 浏览器缩放，不禁用缩放。
- 正文与背景至少满足 WCAG 2.2 AA 的 `4.5:1`，大文本至少 `3:1`，焦点与组件边界至少 `3:1`。
- 所有关键操作支持键盘，图标按钮具有可读 `aria-label`，状态不只依赖颜色。
- 移动端支持安全区、横竖屏、软键盘和触控反馈。

## 11. 禁止项

- 不使用渐变文字、玻璃拟态、重复条纹背景或装饰性模糊光斑。
- 不使用彩色粗侧边条、过度阴影、32px 以上容器圆角或漂浮卡片海洋。
- 不使用营销式 Hero、虚构 KPI、虚构业务记录、占位链接或无功能控件。
- 不混用图标库，不用 Emoji 替代产品图标。
- 不为移动端隐藏核心功能，不以默认值或历史值掩盖接口失败。
- 不读取、输出或记录 `.env`、凭据、密码、Cookie、Token 或验证码。

## 12. 新页面交付清单

新增或重做页面必须逐项确认：

1. 已复用 `base.html` 壳层、`navigation.py` 导航和本文件令牌。
2. 中文使用思源黑体，英文与数字使用 Inter，本地字体引用和缓存键已更新。
3. 页面具备标题、说明、主操作、加载、空、错误、成功和权限状态。
4. 表单有可见标签、明确错误、键盘顺序和重复提交保护。
5. 表格在桌面可扫描，在移动端已重排或局部滚动。
6. 桌面、平板、`320px` 手机、横屏和 200% 缩放均已检查。
7. 焦点可见、触控目标至少 44px、状态不只依赖颜色。
8. 未新增在线字体、第二套图标、虚构数据、隐式业务兜底或敏感信息输出。
9. 已执行相关单元测试、模板静态检查和真实浏览器视觉验收。
10. 若结构、路由或资产位置发生变化，已同步更新 `AGENTS.md`、`CLAUDE.md` 与 `.impeccable/design.json`。
