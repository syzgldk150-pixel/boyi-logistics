# 融辉 TMS 指定侧边栏页面结构与后台逻辑索引

更新时间：2026-03-25

## 1. 文档说明

- 本页只保留全局架构、菜单层级、模块文档入口和特殊说明。
- 详细的 DOM 结构、接口链路、参数映射、保存逻辑已经拆到 `http_service/tms_docs/` 下的模块文档。

## 2. 全局架构

- 主壳页：`/module/index?mv=index`
- 业务页承载：`/widget/home?authenticationKey=<AUTH_KEY>&pageId=<PAGE_ID>&_t=<TS>&_winid=<WIN_ID>`
- 主查询通道：`/dataQuery/findPageByCallId?id=<CALL_ID>`
- 详情/联动通道：`/dataQuery/findAllByCallId?id=<CALL_ID>`
- 字典通道：`/minic/combobox?optionCode=...`
- 保存通道：`/dataOperation/saveTables`
- 列配置通道：`/userView/getUserViewByUrl`
- 前端框架特征：`MiniUI`，大量使用 `xxx$text` / `xxx$value` 双字段。

## 3. 模块文档入口

- [运单管理](./tms_docs/waybill_management.md)：叶子页 `14` 个，分组节点 `1` 个
- [扫描管理](./tms_docs/scan_management.md)：叶子页 `19` 个，分组节点 `1` 个
- [签收管理](./tms_docs/sign_management.md)：叶子页 `3` 个，分组节点 `0` 个
- [预付款管理](./tms_docs/prepayment_management.md)：叶子页 `11` 个，分组节点 `0` 个
- [客服管理](./tms_docs/customer_service_management.md)：叶子页 `11` 个，分组节点 `2` 个
- [投诉管理](./tms_docs/complaint_management.md)：叶子页 `3` 个，分组节点 `0` 个
- [回单管理](./tms_docs/receipt_management.md)：叶子页 `10` 个，分组节点 `0` 个

## 4. 菜单层级

### 运单管理

- `运单录入`：叶子页面
- `订单推送查询`：叶子页面
- `订单管理`：叶子页面
- `运单基础资料修改`：叶子页面
- `寄件运单查询`：叶子页面
- `派件运单查询`：叶子页面
- `延迟扣款管理`：分组节点，下挂 `延迟扣款申请`、`延迟扣款确认`
- `主面单打印`：叶子页面
- `电子标签打印`：叶子页面
- `回单打印`：叶子页面
- `子单绑定主单查询`：叶子页面
- `打印日志查询`：叶子页面
- `运单数据修记录`：叶子页面

### 扫描管理

- `收件扫描`：叶子页面
- `发件扫描`：叶子页面
- `到件扫描`：叶子页面
- `派件扫描`：叶子页面
- `收件-发件扫描`：叶子页面
- `到件-派件扫描`：叶子页面
- `扫描记录查询`：叶子页面
- `到件预报查询`：叶子页面
- `派件预报`：叶子页面
- `收件清单`：叶子页面
- `发件清单`：叶子页面
- `到件清单`：叶子页面
- `留仓件清单`：叶子页面
- `派件清单`：叶子页面
- `对比监控`：分组节点，下挂 `有到未发查询`、`有发未到查询`、`应到件查询`
- `网点到离港时间`：叶子页面
- `网点到离港记录-新`：叶子页面

### 签收管理

- `签收录入`：叶子页面
- `签收查询`：叶子页面
- `是否签收查询`：叶子页面

### 预付款管理

- `预付款账户管理`：叶子页面
- `预付款余额查询`：叶子页面
- `结算明细录入`：叶子页面
- `结算明细查询`：叶子页面
- `固定费用设置`：叶子页面
- `预付款充值申请`：叶子页面
- `预付款提现申请`：叶子页面
- `预付款扫码充值`：叶子页面
- `网商结算账户维护`：叶子页面
- `代收货款查询结果`：叶子页面
- `代收额度明细查询`：叶子页面

### 客服管理

- `工单管理`：分组节点，下挂 `工单登记`、`工单接收方查询`
- `快件跟踪`：叶子页面
- `快件跟踪新`：叶子页面
- `费用调整`：分组节点，下挂 `费用调整登记`、`费用调整接收方查询`
- `公告查询`：叶子页面
- `无头件登记`：叶子页面
- `无头件认领`：叶子页面
- `客服记录查询`：叶子页面
- `云呼系统`：叶子页面

### 投诉管理

- `投诉方登记`：叶子页面
- `被投诉方查询`：叶子页面
- `申诉登记`：叶子页面

### 回单管理

- `回单查询`：叶子页面
- `寄方回单查询`：叶子页面
- `派方回单查询`：叶子页面
- `寄方回单跟踪`：叶子页面
- `派方回单处理`：叶子页面
- `围板箱管理`：叶子页面
- `围板箱调拨发起`：叶子页面
- `围板箱调拨确认`：叶子页面
- `寄方回箱跟踪`：叶子页面
- `派方回箱处理`：叶子页面

## 5. 特殊说明

- `运单管理 -> 延迟扣款管理`、`扫描管理 -> 对比监控`、`客服管理 -> 工单管理`、`客服管理 -> 费用调整` 都是分组节点，不是业务页。
- `无头件登记` 没有稳定标题/pageId，已在 `customer_service_management.md` 中按 DOM 与保存链路单独说明。
- `云呼系统` 当前 live 结果与 `客服记录查询` 共用同一 `pageId`，已在 `customer_service_management.md` 中标注为特殊入口。

## 6. 共页/别名入口

- `客服管理 / 云呼系统`、`客服管理 / 客服记录查询` 共用同一 `pageId`。
