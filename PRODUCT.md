# Product

## Register

product

## Users

物流后台操作员、管理员和业务负责人会在桌面与移动设备上处理查单、回单、录单、调度、自动化、AI 助手只读查询和账号维护。移动场景通常发生在仓库、车辆旁或跨地点协作期间，用户需要在短时间内确认真实状态并完成下一步操作。

## Product Purpose

博益物流控制台把本地业务数据、独立插件和第三方 TMS 数据入口集中到一个受控后台。普通录入与查询直接调用业务接口；手动、定时和固定飞书指令直接调用插件，保存本次执行结果，失败或取消后结束，登录和服务重启不重放旧调用。执行记录可在所属页面按需查看，历史事项只保留查询和核验。固定 AI 助手复用“智能模型”当前激活配置，提供自然中文交流、已注册只读查询和显式财务分析；模型未配置、超时或供应商异常时直接说明原因，不使用离线模拟或隐式降级。韵达与融辉活动原页通过独立来源、一次性 ticket 和路径限定 capability 运行；旧 Console 同源代理保持禁用。

功能插件在“自动化”页管理，财务和客服采集插件分别在所属模块的“数据源管理”中安装、升级、启停、同步、设置和卸载；底层复用同一个插件系统。只有账号需求的插件使用宿主默认账号页，复杂业务配置使用隔离的专属设置页。业务历史和人工处理字段属于业务模块，采集插件停用或卸载后仍可查询。AI 声明可选；已有 AI 能力保留，但模型故障不阻塞固定按钮、飞书指令和明确的业务查询。已有写入路径继续核对工具专属精确预览、当前确认及写后证据。

第一轮按 V3.2 完成低维护边界：常改值从简单设置修改，复杂字段或局部业务判断由 Codex 修改对应插件并运行局部测试和打包；公共接口、共享数据库或运行器改变才进行核心更新。不新增流程编辑器、自然语言查数、知识库或自动修复平台。具体维护归属与本轮验证边界见 `docs/low_maintenance_v32.md`。

自动化页点击后先立即显示列表骨架，再异步补齐安全状态；外部账号、飞书和权限接口异常不能阻止任务名称与基本状态出现。插件明确显示启动、执行、取消与最终结果；当前资源不足时本次失败，不进入积压队列。打开运行输出时读取真实执行记录，刷新或重访可继续查看和取消同一次活跃调用。候选读取、登录、资源、结构和写入未知必须使用不同的中文原因，不能用“执行未完成”掩盖只读预检查失败。

## Brand Personality

克制、清晰、可靠。界面直接说明状态、动作和结果，保持物流调度台应有的秩序与效率，不使用营销语气或装饰性噪音。

## Anti-references

不要做成营销落地页、装饰性卡片堆叠、低信息密度仪表盘或依赖隐藏功能的移动端。不要用模糊兜底掩盖接口失败，不要把 Cookie、Token、密码、验证码或第三方登录态暴露到页面和日志中。

## Design Principles

插件迁移是超级管理员的低频维护操作，从系统状态进入，复用实际插件配置和迁移证据。普通自动化和数据源页面保持日常安装、运行、设置与卸载入口。

- 任务先于装饰：查单、回单、调度和自动化操作在每个设备上都应可直接完成。
- 插件归所属业务：功能插件在自动化，采集插件在财务或客服；复用安装、设置、调度和生命周期，不增加全局插件中心。
- 真实状态可追溯：本地数据、Agent 同步和第三方原页必须可区分，失败原因要明确。
- 结果清楚可查：普通操作直接返回结果，插件记录保留来源、时间和状态；查看记录不再次执行脚本。
- AI 助手安全可见：页面只展示自然中文对话，不展示内部过程、工具摘要、证据面板或常驻连接状态条；明确当前会话仅在服务进程内保存，模型未启用或暂时无法连接时给出中文操作建议。
- 写入范围明确：插件只使用已授权账号与资源；需要预览的业务确认本次实际范围，写入后仍独立核验。
- 高密度仍可扫读：表格、筛选与表单服务于连续处理，而不是压缩到不可用。
- 移动端重排而非缩小：在窄屏中以拇指可达的导航、单列内容和工作区全屏模式保留核心能力。
- 安全与包容默认开启：不回显敏感信息，支持键盘、屏幕阅读器、减少动效和足够对比度。

## Accessibility & Inclusion

遵循 WCAG 2.2 AA。文本与交互控件保持足够对比度，所有关键操作可通过键盘完成，焦点清晰可见，状态不只依赖颜色。移动端触控目标不小于 44px，底部安全区、横竖屏、软键盘和 `prefers-reduced-motion` 均受支持。
