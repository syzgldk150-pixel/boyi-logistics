# Product

## Register

product

## Users

物流后台操作员、管理员和业务负责人会在桌面与移动设备上处理查单、回单、录单、调度、自动化、AI 助手只读查询和账号维护。移动场景通常发生在仓库、车辆旁或跨地点协作期间，用户需要在短时间内确认真实状态并完成下一步操作。

## Product Purpose

博益物流控制台把本地业务数据、自动化能力和受治理的第三方 TMS 数据入口集中到一个受控后台。控制平面在后台把跨入口命令统一保存为可恢复的业务事项、运行步骤、审批和证据；日常状态与操作回到各业务页面，历史与异常只在按需深链中查看，不占用主导航。固定 AI 助手复用“智能模型”当前激活配置，提供自然中文交流和六类只读业务查询；模型未配置、超时或供应商异常时直接说明原因，不使用离线模拟或隐式降级。韵达与融辉活动原页通过独立来源、一次性 ticket 和路径限定 capability 运行；旧 Console 同源代理保持禁用。

插件与自动化使用同一产品入口。用户在“自动化”页安装、升级、启停、执行、定时和卸载插件实例，Console 保留运行治理；插件自己的业务字段在隔离的专属设置页中配置。所有插件必须向 AI 助手声明至少一项中文能力，只有已配置、已启用且稳定的实例可被自然语言调用；查询与计算可直接执行，写入缺少工具专属精确预览和当前会话确认时必须拒绝。

## Brand Personality

克制、清晰、可靠。界面直接说明状态、动作和结果，保持物流调度台应有的秩序与效率，不使用营销语气或装饰性噪音。

## Anti-references

不要做成营销落地页、装饰性卡片堆叠、低信息密度仪表盘或依赖隐藏功能的移动端。不要用模糊兜底掩盖接口失败，不要把 Cookie、Token、密码、验证码或第三方登录态暴露到页面和日志中。

## Design Principles

- 任务先于装饰：查单、回单、调度和自动化操作在每个设备上都应可直接完成。
- 插件即自动化：安装、实例、设置、调度、运行和卸载使用同一张自动化卡片，不向用户暴露第二个扩展管理中心或统一业务配置表单。
- 真实状态可追溯：本地数据、Agent 同步和第三方原页必须可区分，失败原因要明确。
- 事项先于脚本：用户处理的是带责任人、SLA、计划、审批和证据的事项，不需要从脚本日志猜测业务进度。
- AI 助手安全可见：页面只展示自然中文对话，不展示内部过程、工具摘要、证据面板或常驻连接状态条；明确当前会话仅在服务进程内保存，模型未启用或暂时无法连接时给出中文操作建议。
- 高风险动作分离：提交计划不等于批准或执行；影响第三方业务的动作必须明确展示账号、影响范围、计划哈希与写后验证。
- 高密度仍可扫读：表格、筛选与表单服务于连续处理，而不是压缩到不可用。
- 移动端重排而非缩小：在窄屏中以拇指可达的导航、单列内容和工作区全屏模式保留核心能力。
- 安全与包容默认开启：不回显敏感信息，支持键盘、屏幕阅读器、减少动效和足够对比度。

## Accessibility & Inclusion

遵循 WCAG 2.2 AA。文本与交互控件保持足够对比度，所有关键操作可通过键盘完成，焦点清晰可见，状态不只依赖颜色。移动端触控目标不小于 44px，底部安全区、横竖屏、软键盘和 `prefers-reduced-motion` 均受支持。
